import VotingListComponent from '@/components/voting/VotingList';
import { FC } from 'react';

const VotingList: FC = () => {
  return <VotingListComponent type="user" />;
};

export default VotingList;
